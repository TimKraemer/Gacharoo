import { getDefaultMascotPublicPath } from "@/lib/brand/resolve-mascot-path"

/** Public URL for the default Gacharoo mascot avatar (PNG when generated, else SVG). */
export const DEFAULT_GACHAROO_AVATAR_PATH = getDefaultMascotPublicPath()
