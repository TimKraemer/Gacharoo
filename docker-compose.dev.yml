services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: gacharoo
      POSTGRES_PASSWORD: gacharoo
      POSTGRES_DB: gacharoo
    volumes:
      - postgres_dev_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U gacharoo"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_dev_data:
